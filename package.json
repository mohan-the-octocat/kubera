{
  "name": "kubera",
  "version": "0.1.0",
  "description": "Antigravity UI plugin: per-thread token consumption by model, with a FinOps cost view built on published vendor list prices.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "package": "bash scripts/package.sh",
    "capture-fixture": "node tests/capture_fixture.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
